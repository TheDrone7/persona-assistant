import 'package:flutter/material.dart';

class FusionsTab extends StatelessWidget {
  const FusionsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Fusions Tab'),
    );
  }
} 