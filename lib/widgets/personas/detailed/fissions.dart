import 'package:flutter/material.dart';

class FissionsTab extends StatelessWidget {
  const FissionsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Fissions Tab'),
    );
  }
} 